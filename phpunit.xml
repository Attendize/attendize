<?xml version="1.0" encoding="UTF-8"?>
<phpunit backupGlobals="false"
         backupStaticAttributes="false"
         bootstrap="vendor/autoload.php"
         colors="true"
         convertErrorsToExceptions="true"
         convertNoticesToExceptions="true"
         convertWarningsToExceptions="true"
         processIsolation="false"
         stopOnFailure="false">
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>

        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <filter>
        <whitelist processUncoveredFilesFromWhitelist="true">
            <directory suffix=".php">./app</directory>
        </whitelist>
    </filter>
    <php>
        <server name="APP_ENV" value="testing"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="CACHE_DRIVER" value="array"/>
        <server name="MAIL_DRIVER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>

        <!-- DATABASE CONFIG -->

        <server name="DB_PORT" value="3306"/>
        <!--
            Use this with the mysql command line to create the test database

            CREATE DATABASE attendize_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
        -->

        <server name="DB_DATABASE" value="attendize_test"/>

        <!--
            Use this with the mysql command line to create the test user

            CREATE USER 'attendize'@'%' IDENTIFIED BY 'attendize';
            GRANT ALL ON *.* TO 'attendize'@'%';
        -->
        <server name="DB_USERNAME" value="attendize"/>
        <server name="DB_PASSWORD" value="attendize"/>
    </php>
</phpunit>
